apiVersion: v1
kind: Service
metadata:
  name: {{ include "vidiq-app.fullname" . }}
  labels:
    {{- include "vidiq-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vidiq-app.selectorLabels" . | nindent 4 }}
