apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vidiq-app.fullname" . }}-test-connection"
  labels:
    {{- include "vidiq-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vidiq-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
